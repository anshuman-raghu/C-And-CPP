#include<stdio.h>

int
main ()
{
  int a, b, c,d, l;
  scanf ("%d%d%d%d", &a, &b, &c, %d);
  
  if (a>b)
  {
      if (a>c)
      {
          if(a>d)
          {
              if
          }
          
      }
      
      
  }
  
  
  

  printf ("the largest number is %d", l);
  return 0;
}
